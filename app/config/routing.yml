# Internal routing configuration to handle ESI
#_internal:
#    resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#    prefix:   /_internal
KodifyAdminBundle:
    resource: "@KodifyAdminBundle/Controller/"
    type:     annotation
    prefix:   /

login_check:
    pattern: /login_check

logout:
    pattern: /logout

